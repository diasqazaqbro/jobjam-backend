# 🔍 Детальное логирование AI откликов

## Что добавлено

### 1. 📊 Подробное логирование всех этапов

Backend теперь логирует **каждый шаг** процесса AI отклика:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 НАЧАЛО ОБРАБОТКИ AI ОТКЛИКА
   Job ID: 1
   Application ID: abc123
   User ID: user123
   Vacancy ID: vacancy456
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 ШАГ 1/7: Обновление статуса на PROCESSING...
✅ Статус обновлен

📋 ШАГ 2/7: Получение данных вакансии...
✅ Вакансия найдена:
   Название: "Backend Developer"
   Компания: Tech Company
   Город: Алматы
   Зарплата: 500000 - 800000 KZT

👤 ШАГ 3/7: Получение данных пользователя...
✅ Пользователь найден:
   Имя: Иван Иванов
   Email: ivan@example.com

🔑 ШАГ 4/7: Получение HH access token...
✅ Token получен (длина: 1234 символов)

🤖 ШАГ 5/7: ГЕНЕРАЦИЯ РЕЗЮМЕ С ПОМОЩЬЮ AI...
   Отправка запроса к OpenAI GPT-4o-mini...

✅ AI РЕЗЮМЕ СГЕНЕРИРОВАНО:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   📌 Название: "Backend Developer"
   🎯 Навыки (15): Python, Django, PostgreSQL, Redis, Docker...
   💼 Опыт работы (2 мест):
      1. Senior Backend Developer в Tech Corp
         2020-01-15 - настоящее время
         Описание: Разработал и внедрил микросервисную архитектуру...
      2. Backend Developer в StartupXYZ
         2018-03-01 - 2019-12-31
         Описание: Оптимизировал производительность API...
   🎓 Образование: КазНУ им. аль-Фараби (2017)
      Факультет: Информационные технологии
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📤 ШАГ 6/7: СОЗДАНИЕ РЕЗЮМЕ В HEADHUNTER...
   Отправка resume_profile payload в HH API...
   Entry point: vacancy_response
   Vacancy ID: 12345

✅ Резюме создано в HH!
   HH Resume ID: abc123xyz
   URL: https://hh.kz/resume/abc123xyz

📨 ШАГ 6.5/7: ОТПРАВКА ОТКЛИКА В HH...
   Vacancy ID: 12345
   Resume ID: abc123xyz
   Cover letter: да

✅ Отклик успешно отправлен в HH!

💾 ШАГ 7/7: Сохранение резюме в локальную БД...
✅ Резюме сохранено локально (ID: resume123)

🔄 Обновление статуса отклика на COMPLETED...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ ✅ ✅ ОТКЛИК УСПЕШНО ЗАВЕРШЕН! ✅ ✅ ✅
   Job ID: 1
   Application ID: abc123
   Local Resume ID: resume123
   HH Resume ID: abc123xyz
   HH Resume URL: https://hh.kz/resume/abc123xyz
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 2. ❌ Логирование ошибок

При ошибках теперь видно все детали:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ ❌ ❌ ОШИБКА ОБРАБОТКИ ОТКЛИКА ❌ ❌ ❌
   Job ID: 1
   Application ID: abc123
   Ошибка: Failed to get resume ID from HeadHunter response
   Stack: Error: Failed to get resume ID...
       at AIApplicationProcessor.handleAIApplication (...)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Статус отклика обновлен на FAILED
```

### 3. 🤖 Детальное логирование AI генерации

В `OpenAIService`:

```
🤖 Генерация резюме с AI для вакансии: Backend Developer
   Модель: gpt-4o-mini
   Температура: 0.7

✅ Получен ответ от OpenAI (2345 символов)

📋 Парсинг JSON ответа...
   Поля в ответе: title, skills, experience, education

🔧 Нормализация сгенерированного резюме...
   ✅ Навыки: 15 шт.
   ✅ Опыт работы: 2 мест
   ✅ Образование: КазНУ им. аль-Фараби (2017)

✅ Резюме успешно сгенерировано и нормализовано
```

### 4. ⚠️ Логирование fallback

Если AI не сработал:

```
❌ Ошибка генерации резюме с AI:
   Сообщение: OpenAI API timeout
   Тип: TimeoutError

⚠️ Использую fallback генерацию резюме

⚠️ ⚠️ ⚠️ ИСПОЛЬЗУЮ FALLBACK ГЕНЕРАЦИЮ РЕЗЮМЕ ⚠️ ⚠️ ⚠️

📋 Fallback резюме создано:
   Title: Backend Developer
   Skills: 12 шт.
   Experience: 2 мест
   Education: Казахский национальный университет им. аль-Фараби
```

## Как использовать логи

### 1. Просмотр логов в реальном времени:

```bash
# Backend логи
tail -f /tmp/backend.log

# Или через Docker (если используется)
docker logs -f jobjam-backend
```

### 2. Поиск конкретного отклика:

```bash
# По Job ID
grep "Job ID: 1" /tmp/backend.log

# По Application ID
grep "Application ID: abc123" /tmp/backend.log

# По пользователю
grep "User ID: user123" /tmp/backend.log
```

### 3. Поиск ошибок:

```bash
# Все ошибки
grep "❌" /tmp/backend.log

# Только AI ошибки
grep "ОШИБКА ОБРАБОТКИ ОТКЛИКА" /tmp/backend.log

# Fallback случаи
grep "FALLBACK" /tmp/backend.log
```

### 4. Анализ AI генерации:

```bash
# Все AI генерации
grep "AI РЕЗЮМЕ СГЕНЕРИРОВАНО" /tmp/backend.log

# Детали резюме
grep -A 20 "AI РЕЗЮМЕ СГЕНЕРИРОВАНО" /tmp/backend.log
```

## Примеры полезных команд

### Подсчет успешных откликов:
```bash
grep -c "ОТКЛИК УСПЕШНО ЗАВЕРШЕН" /tmp/backend.log
```

### Подсчет ошибок:
```bash
grep -c "ОШИБКА ОБРАБОТКИ ОТКЛИКА" /tmp/backend.log
```

### Среднее время обработки:
```bash
# Смотрим timestamps начала и конца
grep -E "(НАЧАЛО ОБРАБОТКИ|ОТКЛИК УСПЕШНО ЗАВЕРШЕН)" /tmp/backend.log
```

### Последние 10 откликов:
```bash
grep "НАЧАЛО ОБРАБОТКИ AI ОТКЛИКА" /tmp/backend.log | tail -10
```

## Улучшенный AI промпт

Теперь AI получает **подробные инструкции**:

- ✅ Фокус на **конкретных достижениях с метриками**
- ✅ Использование **активных глаголов** (разработал, внедрил, оптимизировал)
- ✅ **Минимум 300 символов** в описании опыта работы
- ✅ Примеры **хороших и плохих** формулировок
- ✅ Требование использовать **реальные казахстанские университеты**

### Пример улучшенного описания:

**Раньше:**
```
Занимался разработкой backend системы
```

**Теперь:**
```
Разработал и внедрил микросервисную архитектуру на Python/Django, 
что сократило время обработки API запросов на 45% и увеличило 
производительность системы с 1000 до 5000 запросов в секунду. 
Оптимизировал работу с PostgreSQL, внедрил кеширование с Redis, 
что снизило нагрузку на БД на 60%. Настроил CI/CD pipeline 
с Docker и Kubernetes, сократив время деплоя с 2 часов до 15 минут.
```

## Мониторинг в production

### Настройка логов для production:

1. **Уровни логирования:**
```env
LOG_LEVEL=info  # debug, info, warn, error
```

2. **Ротация логов:**
```bash
# Установить logrotate
sudo apt install logrotate

# Конфиг в /etc/logrotate.d/jobjam
/var/log/jobjam/*.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    create 0640 jobjam jobjam
}
```

3. **Интеграция с ELK/Grafana:**
```typescript
// winston или pino для структурированных логов
import { WinstonModule } from 'nest-winston';
import * as winston from 'winston';

WinstonModule.forRoot({
  transports: [
    new winston.transports.File({ filename: 'application.log' }),
    new winston.transports.Console(),
  ],
  format: winston.format.json(),
});
```

## Troubleshooting

### AI генерирует плохие резюме?

1. **Проверь логи:**
```bash
grep -A 30 "AI РЕЗЮМЕ СГЕНЕРИРОВАНО" /tmp/backend.log | tail -50
```

2. **Проверь промпт:**
- Вакансия содержит детали?
- Навыки указаны?
- Требования заполнены?

3. **Проверь API key:**
```bash
grep "OPENAI_API_KEY" /tmp/backend.log
```

### Отклики не отправляются в HH?

1. **Проверь HH token:**
```bash
grep "Token получен" /tmp/backend.log
```

2. **Проверь создание резюме:**
```bash
grep "Резюме создано в HH" /tmp/backend.log
```

3. **Проверь отправку отклика:**
```bash
grep "Отклик успешно отправлен в HH" /tmp/backend.log
```

## Дополнительно

### Экспорт логов для анализа:
```bash
# Последние 1000 строк в файл
tail -1000 /tmp/backend.log > analysis.log

# За сегодня
grep "$(date +%Y-%m-%d)" /tmp/backend.log > today.log

# За последний час
grep "$(date +%H:)" /tmp/backend.log > last_hour.log
```

### Парсинг JSON логов:
```bash
# Если используете структурированные логи
cat /tmp/backend.log | jq 'select(.message | contains("AI РЕЗЮМЕ"))'
```

---

**Дата обновления:** 30 ноября 2025  
**Версия:** 2.3.0  
**Статус:** ✅ Production Ready

